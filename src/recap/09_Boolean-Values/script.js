// Booleans Values
// booleans are another data types they are only two values TRUE or FALSE
// they don't use quotation marks around them "true" or "false"

function welcomeToBooleans() {
    return true;
}
// console.log(welcomeToBooleans()); // true

// true of false can be use in more places not just in functions

// eg
function ourTrueOrFalse(isItTrue) {
    if (isItTrue) {
        return "Yes, it's true"
    }
    return "No, it's not true";
}
ourTrueOrFalse(true) // output 'Yes, it's true'


// node src/recap/09_Boolean-Values/script.js